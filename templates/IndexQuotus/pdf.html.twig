<style type="text/css">
<!--
    table.page_footer {width: 100%; border: none; background-color: #ffffff; border-top: solid 0.3mm black; padding: 2mm}
    h1, .image
	{
		color: #000033;
		text-align: center;
	}
    h2 {color: #000055}
    h3 {
		color: #000077;
		text-decoration: underline;
		font-size: 17px;
	}
	
	blockquote {
		font-size: 37.5px;
		font-style: italic;
	}
	
	#header_title
	{
		font-family: "Script MT Bold", "Time New Roman", serif !important;
	}

	a {
		color: black;
		text-decoration: none;
	}
	
	*
	{
		font-family: times;
	}
	
	.quote {
		vertical-align: middle;
	}
	
	.s-left {
		margin-right: 15px;
	}	
	.s-right {
		margin-left: 15px;
	}
-->
</style>

<page backtop="14mm" backbottom="14mm" backleft="10mm" backright="10mm" style="font-size: 12pt">

    <page_footer>
        <table class="page_footer">
            <tr>
                <td style="width: 33%; text-align: left;">
                    Quotus
                </td>
                <td style="width: 34%; text-align: center">
                    [[page_cu]]/[[page_nb]]
                </td>
                <td style="width: 33%; text-align: right">
                    © Quotus {{ ("2019" == "now"|date("Y")) ? "2019" : "2019 - " ~ "now"|date("Y") }}
                </td>
            </tr>
        </table>
    </page_footer>
</page>

<blockquote>
{{ ("photo/left-quotes-sign.png")|max_size_image([], true) }}
{{ entity.text|raw }}
{{ ("photo/right-quotes-symbol.png")|max_size_image([], true) }}
</blockquote>
<hr>
	{% if entity.isBiography %}
		{% if entity.biography.isFictionalCharacter %}
		<p><b><a href="{{ path('app_indexquotus_fictionalcharacter', {'id': entity.biography.id, 'slug': entity.biography.slug}) }}">{{ entity.biography.title }}</a></b>
		{% else %}
		<p><b><a href="{{ path('app_indexquotus_author', {'id': entity.biography.id, 'slug': entity.biography.slug}) }}">{{ entity.biography.title }}</a></b>
		{% endif %}
		
		{% if entity.source is not empty %}
			- <u><i><a href="{{ path('app_indexquotus_source', {'id': entity.source.id, 'slug': entity.source.slug}) }}">{{ entity.source.title }}</a></i></u></p>
		{% endif %}
	{% else %}
		<p><b><a href="{{ path('app_user_show', {'username' : entity.user.username }) }}">{{ entity.user.username }}</a></b></p>
	{% endif %}